# Example: monorepo

**Archetype:** `monorepo`

pnpm + turbo monorepo. Keep Engineering OS state at the repository root.

This folder mimics a consumer repo after `eos init` + `eos detect`. Application source is omitted on purpose.
