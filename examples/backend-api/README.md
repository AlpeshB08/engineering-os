# Example: backend-api

**Archetype:** `backend`

NestJS-style API service with auth and unit tests.

This folder mimics a consumer repo after `eos init` + `eos detect`. Application source is omitted on purpose.
