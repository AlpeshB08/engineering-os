# Archetype: Monorepo

## Discovery focus

- Workspace tooling (pnpm/nx/turbo)
- Package boundaries and dependency direction
- Where `.engineering-os/` should live (prefer repo root)

## Guidance

Scope Feature Contracts to packages/apps impacted; keep one engine state at the root unless packages are independent products.
