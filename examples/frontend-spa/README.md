# Example: frontend-spa

**Archetype:** `frontend`

React + Vite SPA with API client only. Backend Dependency playbook applies.

This folder mimics a consumer repo after `eos init` + `eos detect`. Application source is omitted on purpose.
